services:
  memory:
    image: agent-core/watcher-pilot:latest
    container_name: sandbox-memory
    user: "1001:1001"
    init: true
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp:size=64m,mode=1777
      - /home/agent/.npm:size=16m,mode=0755
    environment:
      ANTHROPIC_API_KEY: ""
      ANTHROPIC_AUTH_TOKEN: ""
      ANTHROPIC_CONSOLE_KEY: ""
      HOME: /home/agent
      PYTHONDONTWRITEBYTECODE: "1"
      PYTHONUNBUFFERED: "1"
      PYTHONPATH: /app
    volumes:
      - type: bind
        source: ${HOME}/.claude
        target: /home/agent/.claude
        read_only: false
      - type: bind
        source: ${HOME}/.claude.json
        target: /home/agent/.claude.json
        read_only: false
      - type: bind
        source: ${HOME}/Projects/agent-core
        target: /app
        read_only: true
    working_dir: /app
    command: ["python3", "/app/scripts/memory_digest.py"]
    networks:
      - default

networks:
  default:
    driver: bridge
